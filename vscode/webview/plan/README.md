# Plan Tab UI

This folder provides the Plan Activity Bar UI implementation for the webview.

## Organization

- `index.ts` renders sessions, handles input, and posts messages.
- `styles.css` defines the minimal readable styling for the Plan tab.
